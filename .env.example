# Basic Auth credentials gating /documents/* (the internal Product Truth / stakeholder
# viewer — see src/proxy.ts and docs/Marketing-Website.md). Required in every environment,
# including local dev: if either is unset, /documents returns 401 for everyone.
DOCS_BASIC_AUTH_USER=
DOCS_BASIC_AUTH_PASS=
